こちらでは、wikiに記載したことに関連するソースコード等を置いていきます。

かなり乱雑です。

# 覚書

```bash
echo "# -_-" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/numeru55/-_-.git
git push -u origin main

git remote add origin https://github.com/numeru55/-_-.git
git branch -M main
git push -u origin main
```
